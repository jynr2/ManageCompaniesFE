import { IdentificationTypeModel } from './identification-type-model';

describe('IdentificationTypeModel', () => {
  it('should create an instance', () => {
    expect(new IdentificationTypeModel()).toBeTruthy();
  });
});
